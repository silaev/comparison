#Monodb Configuration
spring:
  data:
    mongodb:
      database: comparison
      #username:
      #password:
      host: localhost
      port: 0
  mongodb:
    embedded:
      version: 3.2.2
      features: ONLY_WITH_SSL, ONLY_WINDOWS_2008_SERVER
  main:
    banner-mode: "off"

logging:
  level:
    com.silaev.comparison: DEBUG